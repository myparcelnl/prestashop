<script>
var originalOnBeforeUnload = window.onbeforeunload;
// Blocks the page from being reloaded
window.onbeforeunload = () => '';

document.addEventListener('myparcel_label_ready', (url) => {
  window.onbeforeunload = originalOnBeforeUnload;
  window.location.replace(url.detail);
});
</script>

{{ 'Your label(s) are being generated... Please wait. Do not close this page.' | trans({}, 'domain') }}
