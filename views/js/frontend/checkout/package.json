{
  "name": "@myparcel-prestashop/frontend-checkout",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "run ws:build \"$(pwd)\"",
    "build:dev": "run ws:build:dev \"$(pwd)\"",
    "test": "run ws:test \"$(pwd)\"",
    "test:coverage": "run ws:test:coverage \"$(pwd)\"",
    "test:run": "run ws:test:run \"$(pwd)\""
  },
  "dependencies": {
    "@myparcel-dev/pdk-checkout": "^2.0.0"
  },
  "devDependencies": {
    "@myparcel-prestashop/vite-config": "workspace:*",
    "@types/jquery": "^4.0.1",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
